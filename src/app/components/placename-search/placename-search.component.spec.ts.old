import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PlacenameSearchComponent } from './placename-search.component';

describe('PlacenameSearchComponent', () => {
  let component: PlacenameSearchComponent;
  let fixture: ComponentFixture<PlacenameSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PlacenameSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PlacenameSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
